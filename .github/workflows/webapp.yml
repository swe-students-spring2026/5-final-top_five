name: webapp CI

on:
  push:
    branches: [main]
    paths:
      - 'webapp/**'
      - '.github/workflows/webapp.yml'
  pull_request:
    branches: [main]
    paths:
      - 'webapp/**'
      - '.github/workflows/webapp.yml'

jobs:
  test:
    name: Test webapp (pytest + coverage)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: webapp
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install pipenv
        run: python -m pip install --upgrade pip pipenv

      - name: Install dependencies
        run: pipenv install --dev --deploy

      - name: Run tests with coverage
        run: pipenv run pytest test_app.py --cov=. --cov-report=term-missing --cov-fail-under=80

  # TODO (M7): once webapp/Dockerfile lands, enable this job to build and push
  # the image to Docker Hub. Requires DOCKERHUB_USERNAME and DOCKERHUB_TOKEN
  # secrets to be set on the repository.
  #
  # docker:
  #   needs: test
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.DOCKERHUB_USERNAME }}
  #         password: ${{ secrets.DOCKERHUB_TOKEN }}
  #     - uses: docker/build-push-action@v5
  #       with:
  #         context: ./webapp
  #         push: true
  #         tags: |
  #           ${{ secrets.DOCKERHUB_USERNAME }}/topfive-webapp:latest
  #           ${{ secrets.DOCKERHUB_USERNAME }}/topfive-webapp:${{ github.sha }}
