name: ai-service CI

on:
  push:
    branches: [main]
    paths:
      - 'ai-service/**'
      - '.github/workflows/ai-service.yml'
  pull_request:
    branches: [main]
    paths:
      - 'ai-service/**'
      - '.github/workflows/ai-service.yml'

jobs:
  test:
    name: Test ai-service (pytest + coverage)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ai-service
    env:
      USE_MOCKS: 'true'
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install ffmpeg
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - name: Install pipenv
        run: python -m pip install --upgrade pip pipenv

      - name: Install dependencies
        run: pipenv install --dev --deploy

      - name: Run tests with coverage
        run: pipenv run pytest --cov=. --cov-report=term-missing --cov-fail-under=80

  # TODO (M7): once ai-service/Dockerfile lands, enable this job to build and
  # push the image to Docker Hub. Dockerfile must apt-install ffmpeg.
  # Requires DOCKERHUB_USERNAME and DOCKERHUB_TOKEN secrets.
  #
  # docker:
  #   needs: test
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.DOCKERHUB_USERNAME }}
  #         password: ${{ secrets.DOCKERHUB_TOKEN }}
  #     - uses: docker/build-push-action@v5
  #       with:
  #         context: ./ai-service
  #         push: true
  #         tags: |
  #           ${{ secrets.DOCKERHUB_USERNAME }}/topfive-ai-service:latest
  #           ${{ secrets.DOCKERHUB_USERNAME }}/topfive-ai-service:${{ github.sha }}
